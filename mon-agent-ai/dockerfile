FROM mcr.microsoft.com/playwright:v1.49.0-jammy

WORKDIR /app

# Copier les fichiers de dépendances
COPY package*.json ./

# Installer TOUTES les dépendances (nécessaire pour le build)
RUN npm install

# Copier le reste du code
COPY . .

# Compiler avec esbuild (très rapide, peu de RAM)
RUN npm run build

# On nettoie les dépendances de développement pour gagner de la place (optionnel)
# RUN npm prune --production

# Installer les navigateurs Playwright
RUN npx playwright install chromium

EXPOSE 3000

# Lancer le serveur compilé
CMD ["node", "dist/server.js"]